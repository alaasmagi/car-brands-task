info:
  name: PUT - FormEntry
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/form-entries/{ID}
  body:
    type: json
    data: |-
      {
        "fullName": "FULLNAME VALUE",
        "contactPhone": "CONTACTPHONE VALUE",
        "validDrivingLicense": "BOOLEAN DRIVING LICENSE VALUE",
        "selectedCarIds": [
          "SELECTED CAR UUID VALUE",
          "SELECTED CAR UUID VALUE",
          "SELECTED CAR UUID VALUE"
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
