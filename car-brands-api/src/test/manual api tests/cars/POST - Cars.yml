info:
  name: POST - Cars
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/cars
  body:
    type: json
    data: |-
      {
        "name": "NAME VALUE",
        "parentId": "UUID PARENT ID VALUE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
